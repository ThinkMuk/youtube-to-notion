# 🚀 실사용 가이드 — 빌드부터 첫 강의 기록까지

README가 기능·설정 레퍼런스라면, 이 문서는 **"Windows에서 빌드하고 실제 강의에 투입하기까지"**를
순서대로 따라가는 가이드입니다.

---

## 1단계. Windows에서 exe 빌드하기 (`build.bat`)

**사전 조건**: 빌드하는 PC에만 Python 3.11 이상이 필요합니다.
(빌드가 끝난 배포 폴더를 사용하는 PC에는 **아무것도 설치할 필요가 없습니다.**)

프로젝트 폴더에서 `build.bat`을 실행하면 콘솔에 아래 순서로 진행됩니다:

| 단계 | 화면에 보이는 것                                                 | 예상 소요 |
| ---- | ---------------------------------------------------------------- | --------- |
| 1    | `=== YoutubeLiveNotion Windows build ===` → 가상환경(.venv) 생성 | ~30초     |
| 2    | pip 의존성 설치 로그 (faster-whisper, yt-dlp 등 수십 줄)         | 2~5분     |
| 3    | PyInstaller 빌드 로그 (`INFO: ...` 수백 줄)                      | 2~5분     |
| 4    | `=== ffmpeg 동봉 중 (최초 빌드 시 다운로드, 약 80MB)... ===`     | ~1분      |
| 5    | `=== 빌드 완료 ===` 안내 후 아무 키나 누르면 종료                | —         |

빌드가 끝나면 `dist\YoutubeLiveNotion\` 폴더가 생성됩니다:

```
dist\YoutubeLiveNotion\
  YoutubeLiveNotion.exe   ← 실행 파일 (Python 내장)
  ffmpeg.exe              ← 자동 동봉됨
  config.json.example     ← 설정 템플릿
  ...
```

**이 폴더가 곧 배포본입니다.** USB나 클라우드로 폴더째 복사하면 다른 PC에서 무설치로 실행됩니다.

### 빌드 직후 딱 한 번 할 일

`config.json.example`을 복사해 `config.json`으로 이름을 바꾸고 키를 채웁니다.
이미 키가 채워진 `config.json`이 있다면(예: 개발 환경의 것) 그대로 폴더에 복사하면 끝입니다.

```json
{
  "notion_token": "ntn_...",                          ← Notion 통합 토큰
  "notion_parent_page_id": "3a2a77ff...",             ← 기록할 부모 페이지 ID
  "summarizer_backend": "gemini",
  "gemini_api_key": "...",                            ← Google AI Studio 키 (무료)
  ...
}
```

### 첫 실행 시 흔한 현상 (정상)

- **Windows SmartScreen 경고** ("알 수 없는 게시자") → `추가 정보` → `실행` 클릭
- **백신 검사로 첫 실행이 몇십 초 느림** → PyInstaller exe의 일반적 현상, 한 번 지나면 빨라짐

---

## 2단계. 실전 투입 전 리허설 (강력 권장)

본 강의 라이브 전에 **아무 짧은 한국어 강의 VOD(5~10분)** URL로 아래 3단계 흐름을 한 번 돌려보세요.

- VOD와 라이브는 **완전히 같은 코드 경로**라서, 리허설이 성공하면 라이브도 됩니다.
- Whisper 모델 다운로드(최초 1회, medium 기준 약 1.5GB)가 이때 미리 끝나므로
  실전에서 대기 시간이 없습니다.
- 리허설로 생긴 Notion 페이지는 지우면 됩니다.

---

## 3단계. 실사용 순서

버튼 이름은 **[시작]** / **[종료]** 입니다. 브라우저 시청과 이 앱은 **완전히 독립**입니다 —
앱이 스트림을 직접 수신하므로, 브라우저를 열든 말든 / 순서가 어떻든 기록에 영향이 없습니다.

```
① YoutubeLiveNotion.exe 실행
② 유튜브 URL 칸에 라이브(또는 VOD) 링크 붙여넣기
③ 강의 주제 키워드 입력 (예: Java, Spring, JWT)   ← 선택이지만 강력 권장 (인식률↑)
④ [시작] 클릭                                      ← 이 시점부터 기록 시작 (과거 방송분 소급 X)
⑤ 브라우저에서 편하게 라이브 시청                   ← 앱은 뒤에서 알아서 돌아감
⑥ 강의가 끝나면 [종료] 클릭 → 확인 창에서 "예"
⑦ 상태창에 "완료 — Notion 기록이 마무리되었습니다." 표시되면 끝
⑧ Notion 부모 페이지에서 결과 확인
```

### ④~⑦ 사이 상태창에 표시되는 메시지 순서

정상 흐름이라면 이 순서로 흘러갑니다:

1. `스트림 정보를 가져오는 중...`
2. `Notion 페이지 생성 중...`
3. `캡처 시작됨 (슬라이드 감지 대기 중)`
4. (첫 슬라이드 감지 시) `모델 로딩 중`
   — **최초 1회는 여기서 Whisper 모델 다운로드가 일어나 몇 분 걸립니다.**
   이후에는 `models/` 폴더에 저장되어 즉시 로딩됩니다.
5. 슬라이드가 넘어갈 때마다 `Slide N 업로드 완료 (총 N장)`
6. [종료] 후: `전체 강의 요약 생성 중...` → `Notion에 전체 요약 기록 중...`
   → `전체 스크립트 서브페이지 기록 중...` → `완료 — Notion 기록이 마무리되었습니다.`

### 결과물 (Notion 페이지 구조)

```
📄 <강의 제목>                    ← 강의 1회당 페이지 1개 자동 생성
   🎥 자동 생성된 강의 노트 · 날짜
   📌 Slide 1 · 00:00:00
      [슬라이드 이미지]
      • 요약 불릿 2~4개
   📌 Slide 2 · 00:07:32
      ...
   ─────────────
   📝 전체 요약                   ← 주제 / 핵심 개념 / 정리
   📜 전체 스크립트 — <강의 제목>  ← 하위 페이지: 슬라이드별 Whisper 원문 전사
                                    (요약이 부실할 때를 대비한 백업)
```

---

## 4단계. 2주마다 PC를 옮길 때

새 PC에서 할 일은 **폴더 복사 → exe 더블클릭**, 이게 전부입니다.

```
YoutubeLiveNotion\      ← 이 폴더를 통째로 복사 (USB / 클라우드 드라이브)
  YoutubeLiveNotion.exe
  ffmpeg.exe
  config.json           ← 키가 함께 이동됨
  models\               ← Whisper 모델도 함께 이동 → 재다운로드 없음
```

- `models\` 폴더까지 복사하면 새 PC에서 모델 다운로드 대기가 없습니다.
- 혹시 `ffmpeg.exe`가 누락된 폴더를 받아도, 앱이 첫 실행 시 자동으로 다운로드합니다.

---

## 문제가 생겼을 때

| 증상                                          | 확인할 것                                                                                                                                                         |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 시작하자마자 `[오류] 유튜브 스트림 정보를...` | URL 오타 / 멤버십·비공개 영상 여부. 반복되면 deno 설치 (README "사전 준비물" 참고)                                                                                |
| `[오류] Notion 페이지 생성 실패: ... 404`     | Notion 통합이 부모 페이지에 연결됐는지 (페이지 ⋯ → 연결 → 통합 추가)                                                                                              |
| `Gemini API 오류 429` 반복                    | 무료 티어 분당 한도. 자동 재시도되지만 계속되면 잠시 대기                                                                                                         |
| 슬라이드가 너무 잘게 쪼개짐                   | `config.json`의 `debounce`를 올리거나(전환 확정에 필요한 안정 프레임 수↑), 병합 파라미터인 `min_slide_duration_sec`을 올려 짧은 전환이 앞뒤와 더 잘 병합되게 조정 |
| 슬라이드 전환을 놓침(잘 안 잡힘)              | `change_ratio`를 낮추거나 `debounce`를 낮춰(전환 확정까지 걸리는 프레임 수↓) 더 민감하게 감지하도록 조정                                                          |
| 개별 슬라이드 처리 오류                       | 해당 슬라이드만 건너뛰고 세션은 계속됩니다. 종료 후 전체 스크립트로 내용 보완 가능                                                                                |

상세 설정값 설명은 `README.md`를 참고하세요.
